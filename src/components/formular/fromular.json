{
  "collectionName": "components_formular_fromulars",
  "info": {
    "displayName": "fromular"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string"
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "checkbox",
        "text",
        "hidden",
        "select",
        "textarea"
      ]
    },
    "required": {
      "type": "boolean"
    }
  }
}
